using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using Unity.Notifications.Android;
using System;



public class Notifications : MonoBehaviour
{
    /*public void ActiveNotificacion()
    {
        DateTime fechaActivar = DateTime.Now.AddSeconds(5f);
    }
    private const string idCanal = "canalNotificaciones";

    private void Start()
    {
        //StartCoroutine(PermisoDeNotificacion());
    }
    public void CrearNotificaciones(DateTime fecha)
    {
        AndroidNotificationChannel androidNotificationChannel = new AndroidNotificationChannel()
        {
            Id = idCanal,
            Name = " Canalnotificaciones ",
            Description = "Canal para notificaciones",
            Importance = Importance.Default
        };
        AndroidNotificationCenter.RegisterNotificationChannel(androidNotificationChannel);

        AndroidNotification androidNotification = new AndroidNotification
        {
            Title = "Stamina llena",
            Text = "Notificacion de stamina llena",
            SmallIcon = "Default",
            LargeIcon = "Default",
            FireTime = fecha
        };
        AndroidNotificationCenter.SendNotification(androidNotification, idCanal);


       // IEnumerator PermisoDeNotificacion()
       // {
       //     var request = new PermissionRequest();
       //     while (request.Status == PermissionStatus.RequestPending)
       //     {
       //         yield return null;
       //     }
       //
       //
       // }
    } 
    */
}

